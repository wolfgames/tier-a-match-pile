# Spec: <feature name>

> Copy to `.agents/specs/<feature>.md`, fill it in during **plan mode**, then
> execute it in a fresh session and review the diff against this file before
> marking the work done.

## Goal & success criteria

- What "done" looks like, expressed as verifiable checks.

## Design reference

- Which sections of `docs/GAME-DESIGN.md` this implements or changes.

## In scope / out of scope

- **In:** ...
- **Out:** ...

## Files & interfaces involved

- `path/to/file` — what changes and why.

## Approach

- The plan, step by step.

## Verification

- The exact command(s) or checks that prove it works
  (`bun run typecheck`, `bun run test:run`, `bun run test:e2e`, screenshot).
