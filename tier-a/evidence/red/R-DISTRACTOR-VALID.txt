Rule: R-DISTRACTOR-VALID
Test: it("R-DISTRACTOR-VALID (already green — locked-in regression guard) — a distractor triple (matching no Order) still auto-clears normally", ...)
File: tests/unit/game/orders.test.ts

Command: bunx vitest run tests/unit/game/orders.test.ts
Exit code: 0 for this test (PASS)

Status: GREEN, not red. Auto-clear (R-AUTOCLEAR) already has no concept of
Orders and clears any matching triple unconditionally, so this contract is
already satisfied by the current implementation. Recorded here as a locked-in
regression guard — future Orders work must not break it — rather than as a
red gap (docs/GAME-DESIGN.md#matches — "distractor triples are valid
successes").
