Immediately read `AGENTS.md` for full agent guidelines, workflow, and project context.

## Always-Apply Rules

@.claude/rules.md

(Generated by `@wolfgames/cortex/scripts/setup.mjs` — imports every rule with `alwaysApply: true` from cortex + local. Regenerates on `bun install` or `bun run cortex:setup`.)

## Docs

All documentation lives in `docs/`. Read [`docs/INDEX.md`](docs/INDEX.md) for the routing table. The design source of truth is [`docs/GAME-DESIGN.md`](docs/GAME-DESIGN.md).
